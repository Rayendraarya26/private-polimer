services:
    bbkkp_polimer:
#        deploy:
#            mode: replicated
#            replicas: 4
        build:
            context: .
            dockerfile: ./docker/dockerfile/app.Dockerfile
        restart: unless-stopped
        tty: true
        volumes:
            - ./:/var/www
        networks:
            - bbkkp_network
        ports:
            - "4700-4705:80"

#Docker Networks
networks:
    bbkkp_network:
        external: true
